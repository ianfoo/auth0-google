package main

func QueryGoogleAnalytics(accessToken string) (string, error) {
	// TODO (Phase 1) Make a hard coded query to GA
	// TODO (Phase 2) Allow user to specify viewID, dates, dims, metrics.
	return "", nil
}

// GetNewAccessToken gets a fresh Google access token using the refresh token.
func GetNewAccessToken(refreshToken string) (string, error) {
	return "", nil
}
